# ServiceHub

**ServiceHub** — это веб-приложение для поиска и бронирования услуг мастеров, разработанное специально для интеграции с мессенджером MAX. Платформа позволяет пользователям выбирать роль заказчика или исполнителя, находить подходящих специалистов и управлять своими услугами.

## 🚀 Особенности

- **Двойная роль пользователей**: заказчики и исполнители
- **Поиск и фильтрация мастеров** по городу, категориям и тегам
- **Система бронирования** с выбором временных слотов
- **Управление услугами** для исполнителей
- **Календарь и статистика** для провайдеров
- **Адаптивный дизайн** с поддержкой темной темы
- **Skeleton loading** для улучшения UX

## 🛠 Технологический стек

- **Frontend**: React 19, Vite, Tailwind CSS
- **Маршрутизация**: React Router DOM
- **База данных**: Supabase
- **UI компоненты**: Lucide React (иконки)
- **Календарь**: FullCalendar
- **Уведомления**: React Toastify
- **Контейнеризация**: Docker + Nginx

## 📋 Требования

- **Node.js** 18+
- **npm** или **yarn**
- **Docker** (для контейнеризации)

## 🚀 Быстрый старт

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd servicehub
```

### 2. Установка зависимостей

```bash
npm install
```

### 3. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
```

### 4. Запуск в режиме разработки

```bash
npm run dev
```

Приложение будет доступно по адресу: `http://localhost:5173`

### 5. Сборка для продакшена

```bash
npm run build
```

### 6. Предварительный просмотр сборки

```bash
npm run preview
```

## 🐳 Запуск с Docker

### Сборка образа

```bash
docker build -t servicehub .
```

### Запуск контейнера

```bash
docker run --name servicehub_container -p 3000:80 servicehub
```

Приложение будет доступно по адресу: `http://localhost:3000`

### Запуск с переменными окружения

```bash
  docker build -t servicehub .
```

```bash
  docker run --name servicehub_container -p 3000:80 servicehub
```
Приложение будет доступно по адресу: `http://localhost:3000`

### Обновление приложения
```bash
docker run --name servicehub_container -p 3000:80 \
  -e VITE_SUPABASE_URL=your_supabase_url \
  -e VITE_SUPABASE_ANON_KEY=your_supabase_anon_key \
  servicehub
```


## 📁 Структура проекта

```
servicehub/
├── public/                 # Статические файлы
├── src/
│   ├── components/         # React компоненты
│   │   ├── calendar/       # Компоненты календаря
│   │   ├── common/         # Общие компоненты
│   │   ├── dashboard/      # Компоненты дашборда
│   │   ├── filters/        # Компоненты фильтрации
│   │   ├── forms/          # Формы
│   │   ├── layout/         # Компоненты макета
│   │   ├── modals/         # Модальные окна
│   │   ├── profile/        # Компоненты профиля
│   │   └── providers/      # Компоненты провайдеров
│   ├── hooks/              # Пользовательские хуки
│   ├── pages/              # Страницы приложения
│   ├── Api/                # API функции
│   └── main.jsx            # Точка входа
├── docs/                   # Документация
├── Dockerfile              # Конфигурация Docker
├── package.json            # Зависимости проекта
└── vite.config.js          # Конфигурация Vite
```

## 🎯 Основные страницы

### Для всех пользователей:

- **`/`** - Главная страница (выбор роли)

### Для заказчиков:

- **`/customer/browse`** - Поиск мастеров
- **`/customer/provider/:id`** - Профиль мастера

### Для исполнителей:

- **`/provider/setup`** - Настройка профиля
- **`/provider/dashboard`** - Дашборд исполнителя

## 🔧 Доступные команды

```bash
# Разработка
npm run dev          # Запуск dev сервера

# Сборка
npm run build        # Сборка для продакшена
npm run preview      # Предварительный просмотр сборки

# Линтинг
npm run lint         # Проверка кода ESLint
```

## ⚠️ Важные ограничения

### Функции, недоступные вне мессенджера MAX:

1. **Авторизация через MAX** - аутентификация пользователей работает только в среде мессенджера
2. **Получение данных пользователя** - информация о пользователе (имя, ID) доступна только через MAX API
3. **Уведомления** - push-уведомления и интеграция с чатами MAX
4. **Платежи** - интеграция с платежной системой MAX

### Для локального тестирования:

- Используйте моковые данные для пользователей
- Некоторые функции могут работать в ограниченном режиме
- Полный функционал доступен только в production среде MAX

## 🌐 API и База данных

Приложение использует **Supabase** в качестве backend-as-a-service:

- **Аутентификация**: управление пользователями
- **База данных**: PostgreSQL для хранения данных
- **Real-time**: обновления в реальном времени
- **Storage**: хранение файлов (аватары, изображения)

### Основные таблицы:

- `users` - пользователи системы
- `providers` - профили исполнителей
- `services` - услуги мастеров
- `bookings` - бронирования
- `time_slots` - временные слоты

## 🎨 UI/UX особенности

- **Адаптивный дизайн** - работает на всех устройствах
- **Темная тема** - автоматическое переключение
- **Skeleton loading** - плавная загрузка контента
- **Анимации** - современные переходы и эффекты
- **Доступность** - соответствие стандартам a11y

## 🔍 Отладка

### Логи разработки:

```bash
# Включить подробные логи Vite
DEBUG=vite:* npm run dev

# Проверка сборки
npm run build -- --debug
```

### Проблемы с Supabase:

1. Проверьте переменные окружения
2. Убедитесь в корректности URL и ключей
3. Проверьте настройки CORS в Supabase

## 📝 Лицензия

Этот проект разработан для мессенджера MAX и предназначен для внутреннего использования.
Для вопросов по интеграции с MAX или технических проблем обращайтесь к команде разработки MAX.

---

**Примечание**: Данное приложение оптимизировано для работы в экосистеме мессенджера MAX. Некоторые функции могут быть недоступны при запуске вне этой среды.
