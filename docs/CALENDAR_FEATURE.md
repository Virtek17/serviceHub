# Календарь с управлением слотами

## Реализованные компоненты

### CalendarTab

**Путь:** `src/components/calendar/CalendarTab.jsx`

Компонент календаря с FullCalendar интеграцией:

- Отображение слотов в виде событий
- Клик по дате для создания нового слота
- Клик по событию для редактирования/удаления
- Три вида отображения: день, неделя, месяц
- Цветовая индикация: 🟢 зелёный = свободно, 🔴 оранжевый = занято

**Props:**

- `slots` - массив слотов
- `onAddSlot` - функция добавления слота
- `onEditSlot` - функция редактирования слота
- `onDeleteSlot` - функция удаления слота

### SlotModal

**Путь:** `src/components/modals/SlotModal.jsx`

Модальное окно для управления слотами:

- Поля: дата, время начала, длительность
- Отображение статуса слота (свободен/занят)
- Кнопка удаления для существующих слотов
- Валидация обязательных полей

**Props:**

- `isOpen` - флаг открытия
- `onClose` - функция закрытия
- `onSave` - функция сохранения
- `onDelete` - функция удаления
- `slot` - объект слота (null для нового)
- `isLoading` - флаг загрузки

## Стилизация

### Обновлённые стили в index.css

Календарь полностью стилизован под дизайн проекта:

**Светлая тема:**

- Фон: белый (#FFFFFF)
- Границы: #E0E0E0
- Текст: #0D0D0D
- Акцент: #FF6B9D

**Тёмная тема:**

- Фон: #1E1E1E
- Границы: #404040
- Текст: белый
- Акцент: #FF6B9D

**Кнопки:**

- Основные: #FF6B9D
- Активные: #8B70F6
- Hover эффекты

**События (слоты):**

- Свободные: зелёный (#10B981)
- Занятые: оранжевый (#F59E0B)
- Скруглённые углы, hover эффекты

## Функциональность

### Добавление слота

1. Нажать кнопку "Добавить слот" или кликнуть на дату в календаре
2. Заполнить форму (дата, время, длительность)
3. Нажать "Сохранить"
4. Слот появляется в календаре

### Редактирование слота

1. Кликнуть на событие в календаре
2. Изменить данные в форме
3. Нажать "Сохранить"
4. Изменения отображаются сразу

### Удаление слота

1. Кликнуть на событие в календаре
2. Нажать кнопку "Удалить"
3. Подтвердить удаление
4. Слот исчезает из календаря

## Настройки календаря

### Виды отображения

- **День** - почасовая сетка на один день
- **Неделя** - почасовая сетка на неделю (по умолчанию)
- **Месяц** - обзор месяца с событиями

### Временные настройки

- Рабочие часы: 08:00 - 21:00
- Шаг сетки: 30 минут
- Индикатор текущего времени
- Русская локализация

### Интерактивность

- Клик по пустой ячейке - создание слота
- Клик по событию - редактирование/удаление
- Навигация по датам
- Переключение видов

## Интеграция с ProviderDashboardPage

Добавлены функции:

```javascript
handleAddSlot(formData); // Создание нового слота
handleEditSlot(id, data); // Редактирование слота
handleDeleteSlot(id); // Удаление слота
```

Текущая реализация использует локальное состояние.
TODO: заменить на API вызовы к Supabase.

## Структура данных слота

```javascript
{
  id: string,           // Уникальный ID
  date: string,         // Дата в формате YYYY-MM-DD
  time: string,         // Время в формате HH:MM
  duration: number,     // Длительность в минутах
  available: boolean,   // Доступен ли слот
  client?: string       // Имя клиента (если занят)
}
```

## Улучшения для будущего

1. **API интеграция**

   - Создать API endpoints для CRUD операций со слотами
   - Добавить хук useTimeSlots для работы с БД
   - Синхронизация с Supabase

2. **Функциональность**

   - Drag & drop для перемещения слотов
   - Массовое создание слотов (повторяющиеся)
   - Шаблоны расписания (рабочая неделя)
   - Экспорт расписания в календарь (iCal)

3. **UX улучшения**

   - Toast уведомления вместо alert
   - Анимации при добавлении/удалении
   - Подтверждение перед удалением занятого слота
   - Фильтры (только свободные/только занятые)

4. **Дополнительно**
   - Статистика по слотам
   - Конфликты расписания
   - Напоминания о предстоящих записях
   - Интеграция с услугами (автоматическая длительность)
